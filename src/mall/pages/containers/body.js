import style from 'styled-components';

const body = style.div`
    position: absolute;
    width: 100%;
    bottom: 60px;
    top: 0;
    left: 0;
    overflow: auto;
    overflow-scrolling: touch;
`
export default body